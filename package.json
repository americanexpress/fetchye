{
  "name": "fetchye-project",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "eslint --ignore-path .gitignore --ext js,jsx,md .",
    "test": "yarn test:unit",
    "test:unit": "jest",
    "test:git-history": "commitlint --from origin/beta --to HEAD",
    "test:lockfile": "lockfile-lint --path yarn.lock --allowed-hosts registry.yarnpkg.com --validate-https",
    "posttest": "yarn lint && yarn test:lockfile && yarn test:git-history"
  },
  "devDependencies": {
    "@babel/polyfill": "^7.10.4",
    "@commitlint/cli": "^9.1.1",
    "@commitlint/config-conventional": "^9.1.1",
    "amex-jest-preset-react": "^6.1.0",
    "babel-preset-amex": "^3.4.1",
    "core-js-compat": "3.6.5",
    "eslint": "^7.10.0",
    "eslint-config-amex": "^13.1.0",
    "husky": "^4.2.5",
    "jest": "^26.5.2",
    "lerna": "^3.22.1",
    "lockfile-lint": "^4.3.7"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
